AlertLog {
    border: transparent;
    background: white;

    /* TODO: Make dynamic if an AlertLogEntry is opened */
    max-height: 400px;

    /* Arbitrary */
    border-bottom-left-radius: 4px;
    border-bottom-right-radius: 4px;

}

AlertLog #container {
    background: transparent;
}
