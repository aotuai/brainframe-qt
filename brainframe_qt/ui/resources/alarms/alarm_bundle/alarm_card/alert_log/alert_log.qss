AlertLog {
    border: transparent;
    background: white;

    max-height: 200px;

    /* Arbitrary */
    border-bottom-left-radius: 4px;
    border-bottom-right-radius: 4px;

}

AlertLog #container {
    background: transparent;
}
