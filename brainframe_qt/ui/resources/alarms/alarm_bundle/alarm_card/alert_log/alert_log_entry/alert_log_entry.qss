AlertLogEntry > QLabel {
    color: dimgrey;
}

AlertLogEntry:hover {
    background: lightgrey;
}

AlertLogEntry > TextIconButton {
    color: dimgrey;
    font-size: 25px;
    border: none;
}
AlertLogEntry > TextIconButton:checked:hover {
    color: dimgrey;
}
AlertLogEntry > TextIconButton:hover {
    background: darkgrey;
    border-radius: 4px;
}
AlertLogEntry > TextIconButton:focus:pressed {
    background: grey;
    border-radius: 4px;
}

/* ###########################################################################
 * Verified True Button
 * ######################################################################## */

AlertLogEntry > TextIconButton#verified_true_button:checked:!hover {
    color: limegreen;
}
AlertLogEntry > TextIconButton#verified_true_button:!checked:hover {
    color: limegreen;
}

/* ###########################################################################
 * Verified False Button
 * ######################################################################## */

AlertLogEntry > TextIconButton#verified_false_button:checked:!hover {
    color: indianred;
}
AlertLogEntry > TextIconButton#verified_false_button:!checked:hover {
    color: indianred;
}
